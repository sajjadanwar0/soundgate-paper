\* TLC configuration for SoundGate -- ENLARGED BOUND (4 runs x 3 keys).
\* Same Spec, same Safety invariant, same Next as SoundGate.cfg /
\* SoundGate3.cfg; only the finite domains grow. Purpose: a robustness
\* receipt against the "3x3 is tiny" reviewer attack, complementing (not
\* replacing) the TLAPS unbounded-induction tier.
\*
\* Run (from formal/tla/):
\*   java -XX:+UseParallelGC -Xmx6g -cp tla2tools.jar tlc2.TLC \
\*        -workers auto -config SoundGate4.cfg SoundGate.tla \
\*        | tee tlc_4x3.txt
\* Expect: complete search, 0 violations; states in the tens of millions;
\* minutes on the Ryzen workstation. If RAM allows, a 4x4 variant is the
\* same edit with k4 added.
SPECIFICATION Spec

INVARIANT Safety

CONSTANTS
    Runs = {r1, r2, r3, r4}
    Keys = {k1, k2, k3}