\* TLC configuration for SoundGate.
\* Small finite domains suffice: the safety properties are symmetric in run
\* and key ids, so 2 runs x 2 keys already exercises cross-run collisions,
\* fencing, and the zombie-after-close interleavings. Enlarge to {r1,r2,r3} to
\* taste; the state graph grows fast but stays finite.
SPECIFICATION Spec

INVARIANT Safety

CONSTANTS
    Runs = {r1, r2}
    Keys = {k1, k2}